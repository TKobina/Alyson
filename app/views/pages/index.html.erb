<% if user_signed_in? %>
  <h1>Welcome Home, <%= current_user.email %></h1>

  <% if @privileged %>
    <h2> You've got privileges! </h2>
  <% end %>

  </br><%= link_to 'Entities', entities_path %>
  </br><%= link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete } %>



<% else %>
  <h1>Home</h1>
  <%= link_to 'Sign in', new_user_session_path %>
<% end %>

