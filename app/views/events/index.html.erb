<h1>Events</h1>

<ul>
  <% @events.each do |e| %>
    <li>
      <a href="/events/<%= e.id %>">
      <%= e.name %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Event', new_event_path %>
</br><%= link_to 'Home', pages_path %>
